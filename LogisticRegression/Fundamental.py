import numpy as np
import matplotlib.pyplot as plt
import math.exp as exp
import random.random as rand
def cost(theta, features, labels):
    pass

def predict(theta, features):
    pass

def normalization(features):
    pass

def gradientDescent(theta, features, labels, lr = 0.0005, rl_param = 0, ite = 5000):

    features, features_max = normalization(features)
    labels, labels_max = normalization(labels)




